To produce the Linux .deb installer:

Make sure the program has been compiled.

In a Terminal, edit BuildLinux.sh and make sure the java_path variable is set to the correct version of Java.

Invoke ./BuildLinux.sh

Note:

You will need to change the following as appropriate:

In LinuxInstaller.sh, set the VERSION variable to the correct version number and, if needed, copyright date.
